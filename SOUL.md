# SOUL.md - AI Daily Report Generator의 정신

**목적**: 판단 원칙·사명·가치 (WHO·WHY)  
**참고**: CLAUDE.md가 이 문서를 가리킬 때 읽음  
**대상**: 기여자, 개선자, 운영자

---

## 🎯 우리는 누구인가? (WHO)

**AI Daily Report Generator**는 AI 업계의 정보 흐름을 **자동화하고 민주화하는** 프로젝트입니다.

- **개발자**: bwchin98-bot
- **목표**: 매일 최신 AI 동향을 한국어로 정확하게 전달
- **사용자**: 연구자, 개발자, 의사결정자, AI 관심층

---

## 💡 우리가 존재하는 이유 (WHY)

### 문제: 정보 과다와 부실

AI 산업은 매일 수천 개의 새로운 뉴스, 논문, 모델이 나옵니다.
- 어떤 것이 중요한지 알 수 없음
- 좋은 소스들이 흩어져 있음
- 한글 정보 부족
- 매일 수동으로 수집해야 함

### 해결: 자동화된 큐레이션

우리는:
1. **신뢰할 수 있는 소스**에서 데이터 수집 (Hacker News, arXiv, GitHub, Hugging Face)
2. **관련 없는 항목 필터링** (AI 중심)
3. **한글로 번역** (한국 사용자 중심)
4. **컴팩트하게 요약** (1-2줄 = 빠른 읽기)
5. **매일 자동 생성** (수동 작업 제거)

---

## 🌍 우리의 가치 (VALUES)

### 1. 자동화 (Automation)
> "반복되는 일은 기계가 한다"

- 매일 같은 작업을 수동으로 하지 않음
- GitHub Actions로 신뢰할 수 있는 스케줄 실행
- 인간은 분석·판단에만 집중

### 2. 정확성 (Accuracy)
> "출처가 명확하면 신뢰할 수 있다"

- 공식 API 사용 (신뢰성 높음)
- 필터링 키워드 명확하게 공개
- 하드코딩된 데이터도 검증 가능

### 3. 한글화 (Localization)
> "지식은 모국어로 더 깊게 이해된다"

- 100% 한글 번역
- 한국 사용자 중심 (오후 6시 발행)
- Desktop에 자동 저장 (Windows 중심)

### 4. 투명성 (Transparency)
> "어떻게 만들어지는지 누구나 볼 수 있어야 한다"

- GitHub 공개 저장소
- 모든 코드, 설정, 문서 공개
- 데이터 소스 명확
- 변경 이력 기록 (CHANGELOG)

### 5. 접근성 (Accessibility)
> "복잡할수록 더 간단해야 한다"

- `/ai-report generate` 한 줄이면 끝
- 설정 자동화 (setup 스크립트)
- 에러 메시지 친절하게
- 예제 코드 충분하게

### 6. 지속성 (Sustainability)
> "작은 것이 오래간다"

- 외부 의존성 최소화 (npm 패키지 거의 없음)
- Node.js 기본 모듈만 사용
- API 레이트 제한 준수
- 저장소 용량 효율적 (월 750KB)

---

## 📐 설계 철학 (DESIGN PHILOSOPHY)

### 컴팩트 우선 (Compact First)
- 긴 내용은 요약 (1-2줄)
- 자세한 링크는 보존
- 스캔 가능한 구조

### 반응형 (Responsive)
- 모바일에서도 읽기 쉬움
- 테이블릿에 최적화
- 데스크톱에서도 깔끔

### 한국식 (Korean-first)
- 시간대: UTC 9:00 (한국 오후 6시)
- 언어: 한글 기본
- 저장소: Desktop (윈도우 표준)

### 무중단 (Zero Downtime)
- 보고서 생성 실패해도 상관 없음 (다음 날 다시)
- 기존 보고서는 유지
- GitHub 푸시 실패 시 로그만 남김

---

## 🎨 아름다운 코드의 조건

우리가 추구하는 코드:

### 1. 명확함 (Clarity)
```javascript
// ❌ 나쁜 예
const d = fetch(u).then(r => r.json()).then(j => j.m.map(x => ({t:x.by,u:x.u})))

// ✅ 좋은 예
const news = await fetchHackerNewsStories()
  .then(stories => stories.filter(s => s.score > 100))
  .then(topStories => topStories.map(s => createNewsItem(s)))
```

### 2. 일관성 (Consistency)
- 변수 이름: camelCase (JavaScript)
- 함수 이름: 동사 + 명사 (`createCompactNewsItem`)
- 주석: 왜인지만 (무엇인지는 코드가 말함)

### 3. 유지보수성 (Maintainability)
- 함수: 한 가지만 (단일 책임)
- 파일: 500줄 이내
- 모듈: 재사용 가능

### 4. 테스트 가능 (Testable)
- 부수 효과 최소화
- 의존성 명확
- 목(mock)으로 대체 가능

---

## 🚀 우리가 하지 않는 것 (WHAT WE DON'T DO)

### ❌ 예측하지 않음
- 미래 기능을 위해 지금 과설계하지 않음
- 사용될지 모를 옵션 추가하지 않음
- "나중에 필요할 수도"라는 핑계로 복잡성 증가

### ❌ 의존하지 않음
- 거대한 프레임워크 (React, Vue 등)
- npm 패키지 과다 사용
- 외부 서비스 의존성

### ❌ 숨기지 않음
- 에러를 무시하지 않음
- 실패를 조용히 넘기지 않음
- 로그 없이 진행하지 않음

### ❌ 깨뜨리지 않음
- 기존 사용자의 workflow 변경하지 않음
- API 호환성 깨뜨리지 않음
- 역방향 호환성 유지

---

## 🌱 성장 방향

### Phase 1-3: 기초 (완료) ✅
기본적인 데이터 수집과 HTML 생성

### Phase 4: 확장 (예정)
- Email 알림 (구독자 시스템)
- Slack 통합 (팀 공유)
- Twitter 자동 포스팅 (소식 전파)
- Web 대시보드 (과거 보고서 검색)

### Phase 5: 개선 (미래)
- 사용자 맞춤 필터링
- AI 기반 중요도 순위
- 한국 AI 회사 전문 섹션
- 논문 요약 AI 생성

### Phase ∞: 생태계
- 다른 언어 버전
- 다른 산업 버전 (바이오, 양자컴퓨팅)
- 크라우드소싱 큐레이션

---

## 📖 이 정신을 지키려면

### 코드 리뷰 시
- "이 기능이 정말 필요한가?"
- "더 간단하게 할 수 없을까?"
- "지금 추가하거나 6개월 뒤에 추가하거나 차이가 있나?"

### 문제 해결 시
- "자동화할 수 있을까?"
- "한국 사용자 관점은?"
- "모두가 이해할 수 있을까?"

### 배포 시
- "누군가가 깨뜨릴까?"
- "충분히 테스트했나?"
- "문서는 최신인가?"

---

## 🤝 우리와 함께하는 방법

이 정신에 동의한다면:

1. **이슈 제시**: "이게 자동화될 수 있을까?"
2. **PR 기여**: 작고, 명확하고, 목적이 분명한
3. **문서 개선**: 한글, 쉬운 말로
4. **피드백**: "왜 하는지"를 설명해주기

---

## 💭 결국

> **AI Daily Report Generator는 정보를 자동으로 구성하는 도구입니다.**
>
> 우리는 기술로 시간을 절약하고,
> 그 시간으로 더 깊게 생각하기를 믿습니다.

---

**마지막 업데이트**: 2026-06-11  
**철학**: AI 산업의 정보를 민주화하고 자동화한다  
**정신**: 투명하고 단순하고 한글이다
